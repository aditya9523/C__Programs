#include <stdio.h>
#include <stdlib.h>

int main()
{

    char str[20];
    int indexing=0;
    printf("Who are you?");
    scanf("%s",str);
    printf("Welcome Mr ",str[indexing]);
    while(str[indexing]!='\0')
    {
      str[indexing];
    }

}
